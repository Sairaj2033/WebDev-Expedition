<% let isListingPage = typeof currPath !== 'undefined' && currPath.startsWith('/listings/') && currPath !== '/listings/new' && currPath !== '/listings/'; %>
<header class=" sticky-top bg-white border-bottom " style=" top: 0; z-index: 1030; width: 100%;">

<nav class=" navbar navbar-expand-md bg-body-light  px-4 py-3">
    <!-- left -->
 <div class="container-fluid d-flex justify-content-between align-items-center" style="margin-top: 1.7rem;margin-right: 1rem;">

  <a class="navbar-brand" href="/listings"> <img style="height: 2.0rem; margin: 5px;"
        src="/images/airbnb.png"><div style="color: #FF385C;font-size: x-large;display: inline; font-weight:600;">airbnb
             </div></a>

 <!-- </div> -->

<!-- centre  -->
<div style="margin-left: 9rem;">
 <div class="d-none d-md-flex gap-4" >

<div style="">
    <div class="hvr">
        <a href="/listings" class="  nav-link fw-bold text-dark  border-dark border-2 "> <img class="hvr" style="height: 3rem;"  src="/images/house.gif" alt="">  <div> Homes</div></a>
  
    </div>

    <div class="hvr">
         <a href="#" class=" nav-link t   fw-bold text-dark  border-dark border-2 "> <img class="hvr" style="height: 3rem; margin-left: 18px;"  src="/images/hot-air-balloon.gif" alt=""> <div>Experiences</div></a>
 
    </div>

    <div class="hvr">
        <a href="#" class="nav-link   fw-bold text-dark  border-dark border-2"> <img class="hvr" style="height: 3rem; margin-left: 9px; "  src="/images/hotel-ring.gif" alt=""> <div> Services</div></a>

    </div>
</div>

         </div>
         </div>

<% if(currUser) { %>
<div id="compact-search"
class="border rounded-pill shadow-sm d-flex align-items-center px-3 py-2 gap-2 bg-white"
style=" height: 5rem !important;
position: absolute;
left: 50%;
transform: translateX(-50%);
opacity: 0;
pointer-events: none;
transition: all 0.25s ease-in-out;
cursor: pointer;
">            <div class="fw-semibold fs-6 px-2">Anywhere</div>
        <div class="border-end" style="height: 18px;"></div>
        <div class="fw-semibold fs-6 px-2">Any week</div>
        <div class="border-end" style="height: 18px;"></div>
       <div class="text-muted"><a style="font-weight: 600;text-decoration: none; color: black; " href="/listings/new">Add Listing</a></div>
            <button class="btn btn-danger rounded-circle p-2 ms-2 d-flex align-items-center justify-content-center" style="width:35px; height:35px;">
             <a href="/listings/new"> <i class="fa-solid fa-plus text-white"></i></a>   
            </button>
    </div>


<% }%>

    <!-- right -->
     <div class="d-flex align-items-center gap-3">

            <a href="/listings/new" class="nav-link fw-semibold d-none d-sm-block "> Become a host</a>
        
        <button class=" btn btn-light rounded-circle border-0"> <i class="fa-solid fa-globe"></i> </button>
        <div class="dropdown">
            <button class="btn btn-light border rounded-pill d-flex align-items-center gap-2 px-3 py-2 " type="button" data-bs-toggle="dropdown">
                <i class="fa-solid fa-bars"></i>
                <i class="fa-solid fa-circle-user fs-5 text-secondary"></i>
            </button>
            <ul class="dropdown-menu dropdown-menu-en ">

             <% if(!currUser) { %>
                <li> <a  class="dropdown-item" href="/signup">Sign up</a></li>
             <% } %>

             <% if(!currUser) { %>
                 <li> <a class="dropdown-item" href="/signin">Sign in</a></li>
             <% } %>


              <% if(currUser) { %>
                 <li><a class="dropdown-item" href="/logout">LogOut</a></li>
              <% } %>   
            </ul>

        </div>
     </div>

 </div>
</nav>

<!-- 4. SEARCH BAR -->
 <% if (currUser) { %>
<% if (!isListingPage) { %>
    <!-- Added id="big-search" and inline CSS for smooth fading -->
    <div  id="big-search" class="container d-flex justify-content-center py-3" style=" transition: all 0.2s ease-in-out; opacity: 1; transform: scale(1); transform-origin: top;">
        <div class=" navbar border rounded-pill shadow-sm d-flex align-items-center px-4 py-2 gap-3" style=" cursor:pointer;">
            <div class="fw-bold" >Anywhere</div>
            <div class="border-end" style="height: 24px;"></div>
            <div class="fw-bold">Any week</div>
            <div class="border-end" style="height: 24px;"></div>
            <div class="text-muted"><a style="font-weight: 600;text-decoration: none; color: black; " href="/listings/new">Add Listing</a></div>
            <button class="btn btn-danger rounded-circle p-2 ms-2 d-flex align-items-center justify-content-center" style="width:35px; height:35px;">
             <a href="/listings/new"> <i class="fa-solid fa-plus text-white"></i></a>   
            </button>
        </div>
    </div>
<% } %>
 </header>
<% }%>
<!-- Scroll Animation Script -->
<script>
    document.addEventListener("scroll", () => {
        const bigSearch = document.getElementById("big-search");
        const compactSearch = document.getElementById("compact-search");
        const centerTabs = document.getElementById("center-tabs");

        if (window.scrollY > 50) {
            // Scroll down: Hide Big Search & Tabs, Show Compact Pill
            if (bigSearch) {
                bigSearch.style.opacity = "0";
                bigSearch.style.transform = " translateY(-20px)";
                bigSearch.style.pointerEvents = "none";
                bigSearch.style.height = "0"; // Collapses the space
            }
            if (centerTabs) centerTabs.style.opacity = "0";
            if (compactSearch) {
                compactSearch.style.opacity = "1";
                compactSearch.style.transform = "translateX(-50%) ";
                compactSearch.style.pointerEvents = "auto";
            }
        } else {
            // Scroll up: Reset everything
            if (bigSearch) {
                bigSearch.style.opacity = "1";
                bigSearch.style.transform = "scale(1) translateY(0)";
                bigSearch.style.pointerEvents = "auto";
                bigSearch.style.height = "auto"; 
            }
            if (centerTabs) centerTabs.style.opacity = "1";
            if (compactSearch) {
                compactSearch.style.opacity = "0";
                compactSearch.style.transform = "translateX(-50%) ";
                compactSearch.style.pointerEvents = "none";
            }
        }
    });
</script>
